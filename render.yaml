# Render Blueprint configuration
services:
  - type: web
    name: vector-deploy-simple-app-the-app-should-simply-say-687fd6e3
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
